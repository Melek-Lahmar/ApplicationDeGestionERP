@model ApplicationDeGestionERP.Models.G_Facture
@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager

@{
    if (SignInManager.IsSignedIn(User) && User.IsInRole("Admin"))
    {
        Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    }
    else
    {
        Layout = "~/Views/Shared/_Layout.cshtml";
    }
}
@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>G_Facture</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.FactureRef)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.FactureRef)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DateFacture)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DateFacture)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Client)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Client.AdresseClient)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="FactureID" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
